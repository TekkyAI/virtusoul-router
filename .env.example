# ============================================
# VirtuSoul Router Configuration
# ============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Server settings
HOST=0.0.0.0
PORT=4000

# The model name your app will use (e.g. "virtusoul-v1")
# Your app sends: {"model": "virtusoul-v1"} and the router classifies + routes
MODEL_NAME=virtusoul-v1

# ============================================
# Tier Configuration
# ============================================
# Each tier maps to a provider + model + API key.
# Supported providers: openai, anthropic, openrouter, groq, together, ollama, mistral, deepseek, google
#
# You only need to configure the tiers you want to use.
# Unconfigured tiers fall back to the MEDIUM tier.

# --- Simple Tier (greetings, factual lookups, basic math) ---
SIMPLE_PROVIDER=openrouter
SIMPLE_MODEL=meta-llama/llama-3.2-3b-instruct:free
SIMPLE_API_KEY=sk-or-your-openrouter-key
# SIMPLE_BASE_URL=                  # Optional: custom base URL

# --- Medium Tier (explanations, summaries, simple code) ---
MEDIUM_PROVIDER=openrouter
MEDIUM_MODEL=openai/gpt-4.1-mini
MEDIUM_API_KEY=sk-or-your-openrouter-key
# MEDIUM_BASE_URL=                  # Optional: custom base URL

# --- Complex Tier (architecture, system design, refactoring) ---
COMPLEX_PROVIDER=openrouter
COMPLEX_MODEL=anthropic/claude-sonnet-4
COMPLEX_API_KEY=sk-or-your-openrouter-key
# COMPLEX_BASE_URL=                 # Optional: custom base URL

# --- Reasoning Tier (proofs, formal logic, optimization) ---
REASONING_PROVIDER=openrouter
REASONING_MODEL=openai/o4-mini
REASONING_API_KEY=sk-or-your-openrouter-key
# REASONING_BASE_URL=               # Optional: custom base URL

# ============================================
# Optional Settings
# ============================================

# API key to protect your router (optional — if set, clients must send this as Bearer token)
# API_KEY=your-secret-key

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Request timeout in seconds
# TIMEOUT=120
